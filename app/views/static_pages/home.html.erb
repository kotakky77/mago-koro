<% content_for :title, "まごころおくりもの - ホーム" %>

<!-- ヒーローセクション -->
<div class="hero-section">
  <div class="hero-content">
    <div class="connection-visual">
      <div class="family-circle grandparent">
        <i class="fas fa-heart"></i>
        <span>祖父母</span>
      </div>
      <div class="connection-line">
        <div class="heart-bridge">
          <i class="fas fa-heart"></i>
          <i class="fas fa-heart"></i>
          <i class="fas fa-heart"></i>
        </div>
      </div>
      <div class="family-circle parent">
        <i class="fas fa-hands-helping"></i>
        <span>親</span>
      </div>
      <div class="connection-line">
        <div class="heart-bridge">
          <i class="fas fa-heart"></i>
          <i class="fas fa-heart"></i>
          <i class="fas fa-heart"></i>
        </div>
      </div>
      <div class="family-circle child">
        <i class="fas fa-smile"></i>
        <span>孫</span>
      </div>
    </div>
    
    <h2 class="hero-subtitle">愛をつなぐ、まごころのおくりもの</h2>
    <p class="hero-description">家族の絆を深め、離れていても心をつなぐプラットフォーム</p>
  </div>
</div>

<!-- 機能紹介セクション -->
<div class="features-section">
  <div class="container">
    <h2 class="section-title">できること</h2>
    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">
          <i class="fas fa-gift"></i>
        </div>
        <h3>ほしいものリスト</h3>
        <p>親御さんが子供の「ほしいもの」をリストアップ。祖父母の方は孫の喜ぶギフトを簡単に選べます。</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">
          <i class="fas fa-camera"></i>
        </div>
        <h3>成長の共有</h3>
        <p>子供の写真をアップロードして、離れて暮らす祖父母と日々の成長を共有できます。</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">
          <i class="fas fa-star"></i>
        </div>
        <h3>記念品</h3>
        <p>子供の描いた絵をプリントした特別な記念品で、世界にひとつだけの思い出を作りましょう。</p>
      </div>
    </div>
  </div>
</div>

<style>
/* ヒーローセクション */
.hero-section {
  background: linear-gradient(135deg, #FF9E80 0%, #FFB74D 50%, #64B5F6 100%);
  min-height: 60vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 60px 20px;
  position: relative;
  overflow: hidden;
}

.hero-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
}

.hero-content {
  text-align: center;
  position: relative;
  z-index: 2;
  max-width: 800px;
}

/* 家族のつながりビジュアル */
.connection-visual {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
  gap: 20px;
  margin-bottom: 40px;
}

.family-circle {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  background: white;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  animation: float 3s ease-in-out infinite;
  transition: transform 0.3s ease;
}

.family-circle:hover {
  transform: scale(1.05);
}

.family-circle.grandparent {
  background: linear-gradient(135deg, #E1F5FE, #B3E5FC);
  animation-delay: 0s;
}

.family-circle.parent {
  background: linear-gradient(135deg, #F3E5F5, #E1BEE7);
  animation-delay: 1s;
}

.family-circle.child {
  background: linear-gradient(135deg, #FFF3E0, #FFE0B2);
  animation-delay: 2s;
}

.family-circle i {
  font-size: 36px;
  margin-bottom: 8px;
  color: var(--primary-color);
}

.family-circle span {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-color);
}

.connection-line {
  height: 4px;
  width: 80px;
  background: white;
  border-radius: 2px;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.heart-bridge {
  display: flex;
  gap: 15px;
}

.heart-bridge i {
  color: #FF6B6B;
  font-size: 12px;
  animation: heartbeat 2s ease-in-out infinite;
}

.heart-bridge i:nth-child(2) {
  animation-delay: 0.3s;
}

.heart-bridge i:nth-child(3) {
  animation-delay: 0.6s;
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

@keyframes heartbeat {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.2); }
}

/* ヒーローテキスト */
.hero-subtitle {
  font-size: 32px;
  font-weight: 700;
  color: white;
  margin-bottom: 20px;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
}

.hero-description {
  font-size: 18px;
  color: white;
  opacity: 0.95;
  line-height: 1.6;
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
}

/* 機能紹介セクション */
.features-section {
  padding: 80px 0;
  background: white;
}

.section-title {
  text-align: center;
  font-size: 32px;
  font-weight: 700;
  color: var(--text-color);
  margin-bottom: 50px;
  position: relative;
}

.section-title::after {
  content: '';
  position: absolute;
  bottom: -10px;
  left: 50%;
  transform: translateX(-50%);
  width: 60px;
  height: 4px;
  background: var(--primary-color);
  border-radius: 2px;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 40px;
  max-width: 1200px;
  margin: 0 auto;
}

.feature-card {
  text-align: center;
  padding: 40px 30px;
  background: white;
  border-radius: 16px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  position: relative;
  overflow: hidden;
}

.feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 158, 128, 0.1), transparent);
  transition: left 0.6s ease;
}

.feature-card:hover::before {
  left: 100%;
}

.feature-card:hover {
  transform: translateY(-10px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
}

.feature-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto 25px;
  background: linear-gradient(135deg, var(--primary-color), #FFB74D);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8px 20px rgba(255, 158, 128, 0.3);
}

.feature-icon i {
  font-size: 36px;
  color: white;
}

.feature-card h3 {
  font-size: 24px;
  font-weight: 600;
  color: var(--text-color);
  margin-bottom: 20px;
}

.feature-card p {
  font-size: 16px;
  line-height: 1.6;
  color: #666;
}

/* レスポンシブ対応 */
@media (max-width: 768px) {
  .hero-section {
    min-height: 50vh;
    padding: 40px 20px;
  }
  
  .connection-visual {
    flex-direction: column;
    gap: 15px;
  }
  
  .connection-line {
    transform: rotate(90deg);
    width: 60px;
  }
  
  .family-circle {
    width: 100px;
    height: 100px;
  }
  
  .family-circle i {
    font-size: 28px;
  }
  
  .hero-subtitle {
    font-size: 24px;
  }
  
  .hero-description {
    font-size: 16px;
  }
  
  .features-grid {
    grid-template-columns: 1fr;
    gap: 30px;
  }
  
  .feature-card {
    padding: 30px 20px;
  }
}

@media (max-width: 480px) {
  .hero-subtitle {
    font-size: 20px;
  }
  
  .hero-description {
    font-size: 14px;
  }
  
  .section-title {
    font-size: 24px;
  }
  
  .feature-card h3 {
    font-size: 20px;
  }
  
  .feature-card p {
    font-size: 14px;
  }
}
</style>
